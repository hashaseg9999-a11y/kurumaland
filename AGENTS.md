# AGENTS.md — kurumaland

## Repository Scope

このリポジトリは幼児向け知育アプリ `KurumaLand` の開発用です。
変更前に必ず `docs/DESIGN.md` を確認し、そこで定義された設計原則・インターフェース・非機能要件を維持してください。設計から意図的に逸脱する場合は、PR本文に理由を明記してください。

## 公開・変更境界

- `main` へ直接 push せず、トピックブランチから小さなPRを作成すること。
- GitHub Pages で公開される前提で、マージ後の公開影響を確認すること。
- GitHub Actions、Pages設定、Viteの `base`、PWA設定、外部リンク、実行時ネットワーク通信、計測・分析、依存パッケージ追加など、公開・配信・設計境界を変える変更は **Ask First**。
- `docs/DESIGN.md` が定める「広告ゼロ・外部リンクゼロ・実行時ネットワーク通信ゼロ・計測/分析ゼロ」を維持すること。
- 無関係なファイルへ変更範囲を広げないこと。

## 検証ループ

1. 変更前に `docs/DESIGN.md` と対象コードを確認する。
2. 合否判定は `npm run verify` を最優先で使用する。
3. 失敗した場合は `原因分析 → 最小修正 → 再検証` の順で対応する。
4. 同一原因に対する自動retryは最大3回。解決しない場合は試行内容と未解決理由を報告して停止し、Human判断を仰ぐ。
5. 検証を通すためだけに、型チェック・ビルド・既存要件を弱体化しないこと。

## サブエージェント

- 独立して並列化できる作業だけfan-outしてよい。
- 同一ファイルや強い依存関係がある作業は並列化しない。
- 最終的なjoin、`npm run verify`、差分確認、PR作成はParentが行う。
